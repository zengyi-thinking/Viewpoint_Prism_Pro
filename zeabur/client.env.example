HOSTNAME=0.0.0.0
PORT=7870

# Private networking: point to the server service inside the same Zeabur project.
INTERNAL_API_URL=http://server:7871

# Keep empty so HTTP requests go through Next.js rewrite (/api -> server).
NEXT_PUBLIC_API_URL=

# Public backend domain for websocket connection, for example:
# NEXT_PUBLIC_WS_URL=https://viewpoint-prism-api.zeabur.app
NEXT_PUBLIC_WS_URL=
