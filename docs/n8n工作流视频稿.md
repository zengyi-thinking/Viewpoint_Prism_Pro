这车里的信物

你护不住

交出来

你我各走各的路

这是我拿信誉换来的差事

有些分寸不能乱

嗯那看来我得亲自找你要个公道了

从输入一段故事剧本到转化为CDANCE

2.0可以直接稳定使用的视频提示词

中间需要的场景图

角色素材图

分镜参考图全部自动生成

你要做的只是点一下这个按钮

这套N8N工作流会自动识别对白

文戏

打斗武器等不同类型的内容

再生成针对c dance2.0深度优化过的提示词

尽可能把精品视频制作的门槛降到最低

我们先来看一下这套工作流的几个特点

第一你需要输入的内容非常少

只要提供一段故事

再定义一下动画风格就够了

工作流会自动识别角色和场景

生成角色设定图

场景图

把故事拆分成多个连续分镜

自动生成多张分镜九宫格图

并且能保持角色和场景色调等的高度一致性

第二工作流会根据角色的年龄

性别等特征

自动匹配我预制好的多种音色文件

让同一个角色在多段视频里的音色保持一致

第三在拆分分镜时

你可以按自己的喜好去引导AI

比如控制拆分力度等

不再只能接受AI默认的拆分方式

第四最终生成的是针对CDX2.0

深度优化过的视频提示词

除了会区分文戏和武戏

采用不同的优化方式

也会自动处理集梦提示词2000字上限的问题

你只需要把最终生成的提示词复制到集梦里

相关的艾特引用关系

工作流也已经帮你整理好了

后面只要上传图片和音频

做一点简单替换即可和往期一样

工作流和部署文档全部免费分享

另外我也准备了云端一键运行的版本

如果你不想自己折腾N8N部署

开机就能直接用

最大程度帮你节省时间

那这期视频分成两个 部分

第一部分我会系统性的拆解这套工作流的原理

帮你理解清楚它到底是怎么运作的

方便你自己扩展改造

第二部分我会讲解工作流的具体配置方法

以及云端一键运行的使用方式

总有一个适合你好

下面我们先来看这套工作流的整体流程和原理

整个工作流可以分成四个模块

分别是故事中的角色与场景提取以及素材图

生成核心的分镜

图片与视频提示词

生成角色音色自动匹配

调用API生成分镜图

首先看第一部分

这套工作流的核心思路是

先按场景维度去拆分故事

比如你的剧本里有室内转室外的情景

工作流会先把它拆成两个场景

在识别每个场景里涉及到的角色

并根据剧情自动设定场景外观和角色形象

生成后续生图要用到的提示词

之所以先按场景来拆分

是为了让后面的AI在生成具体分镜提示词时

每次只分析一个场景里的内容

这样上下文会更聚焦

生成出来的效果也会更好

那么有了生图提示词

这次我用来生成设定图的模型是Z1妹纸

它的优点是速度快

成本低

并且能完全满足我们做角色图和场景图的需求

接下来看第二部分

分镜提示词生成模块

这个模块是整套工作流的核心

一共用了九个A站的节点配合完成

这个大模块又可以分为两个小模块

分别是生成模块和c dance2.0优化模块

先说生成模块

它和我之前的solo2慢剧工作流思路基本一致

首先会对整篇故事做一次摘要

同时对每个场景对应的内容再做一份摘要

后面再分别生成每个场景的提示词时

会将前后场景及整个故事的摘要加入提示词中

这样AI才能真正理解剧情上下文

保证整体的连贯性

接着就是拆分分镜的环节

这里我是按15秒作为一个视频片段

让AI把一个场景拆成多个15秒片段

每个片段再细分成九个子镜头

也就是后面要生成的九宫格分镜图

整个过程会先搭建一个框架

类似文章先列大纲一样

然后再用专门的agent的节点对大纲做细化

每个分镜补充镜头角度

运镜方式构图声音设计等细节

与此同时

它还会识别文本里出现的所有台词

并为不同角色匹配对应的台词关系

最终这一步会生成每个片段对应的

出版视频提示词以及分镜图提示词

这里有一个很重要的点

就是在拆分大纲时

我专门设计了一个用户自定义提示词模块

也就是视频开头提到的

你可以按自己的喜好补充一些原则

比如大纲拆的更细一点

或者更偏动作表现等等

AI会把你的自定义提示词

当成最高优先级来执行

有了出版提示词之后

接下来就是第二个小模块

针对CDAS2.0的优化模块

C大2.0是一个语义遵循度非常强的模型

如果你给他的是非常细致的分镜提示词

包括每个运镜人物运动方式等

那么它会尽可能遵循所有的提示词

如果你给的提示词是比较笼统的

它也能根据参考图自己演绎出

非常流畅且逻辑合理的视频

我做了大量测试之后

发现打戏片段其实不适合写的太死

很多时候不需要把每个分镜细节都定的特别严

只需要把关键视觉要素和剧情进展交代清楚

模型反而能发挥得更自然

出来的打戏会更丝滑

也更有张力

所以在工作流里会自动判断

每个片段到底是文戏还是武戏

如果是五系

就会额外经过一个agent的节点

在原始提示词基础上继续优化

弱化过细的分镜描述

在不改变剧情逻辑的前提下

融入环境破坏反馈

动态模糊

速度感

这些更适合动作场面的提示词

让模型本身有更大的发挥空间

如果是文戏

就尽量保持原本的细节描述不动

因为文戏通常会涉及人物表演

对白节奏这些内容细节反而更重要

通过这种分开处理的方式

才能更充分地发挥

CDS2.0在不同片段里的优势

再下一步就是角色音色匹配

系统会根据角色形象

再结合我提供的预制音色列表

通过agent节点自动给角色分配合适的音色

并绑定对应的音色参考文件路径

方便你后面上传到G梦

当然这里的音色文件你也可以进行自定义

只需要将文件放到音色文件夹中

并修改音色描述文件即可

这部分细节我会在部署文档中有详细的说明

最后工作流会自动整理和拼接

G梦需要的参考图格式

也就是这种艾特的引用方式

把分镜图角色图音色关系全部对应好

另外因为季梦有2000字的提示词上限限制

所以工作流还会再对完整提示词做一次压缩

在尽量保留关键内容不变的前提下

比如刚才提到的艾特引用关系角色台词

这些信息

把一部分分镜描述进一步压缩

尽可能控制在2000字以内

到这里所有提示词就都准备好了

接下来工作流会调用API去生成九宫格分镜图

这里使用的模型是nano banana pro

在同一个场景中

除了角色素材外

还会将上一张分镜图

作为当前分镜图生成时的参考图

从而保证前后色调构图

逻辑关系等的一致性

在分镜图完成之后

整套素材就准备齐全了

而且也都会自动下载到本地文件夹中

最后工作流还会把视频提示词

以及对应使用的分镜图路径

统一整理成一个excel文件

同样下载到本地

打开之后我们直接把提示词复制到G梦里

参考图一固定使用分镜图

excel里会标明具体对应的图片路径

把这张分镜图作为参考图一一上传即可

然后再根据艾特引用关系

把角色图

音色文件等素材依次上传

这里还有一个细节要注意

在提示词里原本带at的位置

鼠标左键点一下会弹出对应的引用提示

你需要把后面的引用替换成

自己实际上传的素材

再把原本那段文本删掉就可以了

这样才能达到真正的引用效果

最后点击生成整个流程就完成了

下面我们再看一下工作流的配置方法

视频开头提到过配置方式分为两种

本地部署和云端一键运行

我们先说本地部署

本地部署

我建议直接使用我提供的docker镜像

因为N8N本身对工作流版本的兼容性不太好

另外N8N2.0以上的版本

还需要额外配置不少环境变量

直接用我提供的docker镜像去部署

可以一次性避免这些兼容问题

让整个部署过程简单很多

关于docker镜像的安装步骤

我已经放到部署文档里了

里面有很详细的步骤说明

整体并不复杂

你唯一需要额外注意的就是

映射路径

一定要改成你自己本地的实际路径即可

假设你已经成功安装好了

docker镜像

也启动了N8N后面的配置就很简单了

我们需要导入三个子工作流和一个主工作流

三个子工作流主要负责调用API生成图片

主工作流负责串起整个流程

这里顺便说一下

我默认使用的生图API是真真的AI工坊

你也完全可以改成自己习惯的API

甚至换成别的生图模型也没问题

因为真正调用API的逻辑都在子工作流里

节点关系很简单清晰

只要保持子工作流的输入和输出参数不变

中间的调用方式你可以很自由的更改

关于真真的AI工坊APIT获取方式

我也放到部署文档里了

拿到APIT之后

打开文生图和生成分镜图片

这两个子工作流

分别找到发起任务这个HTTP请求节点

双击打开

新建一个认证方式

选择这种认证方式

然后填写APIT即可

然后回到主工作流

主工作流里默认所有的agent的节点

使用的大模型是deep sick

所以你还需要填写deep sick的官方API key

把每个deep sick模型节点的key配置好

接着打开基础字段节点

这里需要分别填写三个子工作流的id

在浏览器地址栏里

workflow后面的那串内容就是对应的工作流id

我们把三个子工作流分别打开

把他们的id复制到对应的位置上

基础配置就完成了

最后打开视频配置节点

这里一共有三个字段

在第一个字段中填写你的故事

剧本内容

在第二个字段中填写艺术风格

比如新海诚风格

吉普利风格等等

第三个字段就是前面提到的分镜大纲拆分时

自定义的偏好提示词

这个字段是选填不填也没问题

OK接下来点击按钮开始运行工作流即可

流程跑完以后

所有素材和整理好的提示词

excel会自动下载到你配置好的本地映射路径中

然后我们再来看一下云端一键运行的版本

这里我用的是先工云

因为这套工作流升图时主要是在调用API

并不吃显卡

所以直接选最低配置就够用了

而且目前通过我的邀请链接

注册限公云还会送八块钱的体验

完全足够玩一阵的了

注册之后直接打开我提供的相公云镜像地址

具体地址在部署文档里也有点击立即部署之后

选择一个显卡配置

直接选最便宜的就行

计费模式

这里选择按小时计费

这样不用的时候你也可以随时停掉

然后点击确认部署即可

稍等一会儿

等服务器显示运行中

就可以点击这个相公云OS的按钮

进入界面之后

点击这个N8N的图标

就会弹出N8N注册界面

这里我们简单填写一下即可

并不会让你去做认证

进入主界面后

点击第二个选项卡

这里我已经预制好了

deep sick和真真的AI工坊的配置

你只需要把自己的APIT填进去

工作流里就不用再一个一个单独配置了

当然如果你平时有自己更常用的API

也可以按自己的习惯去更改

然后回到第一个选项卡

你会看到这里有很多工作流

其中也包含我之前视频里讲过的其他工作流

这次你只需要关注

以c dance2.0开头的这四个工作流

在云端版本里

我已经把所有工作流id和相关配置都预制好了

所以会比本地部署省事很多

你只需要直接打开主工作流

找到视频配置节点

填写故事剧本和艺术风格

然后点击开始运行就可以了

最终生成的文件会放在线工云系统桌面的N8

N输出目录文件夹里

把这些文件全部下载到你自己的电脑上

就可以直接使用

那最后我来分享两个

我在制作工作流中总结的经验

第一也是最重要的一点

就是故事剧本格式的重要性

剧本格式越是清晰易懂

那AI在拆分片段或者生成分镜提示词时

效果就会越好

如果剧本格式本身对于人类来说

读起来还需要一定的脑补才能理解

比如角色对白角色关系等

那AI就会很大概率做出错误的理解

导致生成的提示词跑偏

所以如果你的剧本本身比较难读

可以尝试让AI先对剧本润色一遍

输出一个更清晰易懂的版本

再交给工作流

第二个经验是对于越强大的模型来说

例如CDX2.0

在提示词中可以适当的减少约束

给AI更多的发挥空间

也许效果会更好

当然这需要我们根据实际需求区分

什么场景下需要AI自由发挥

什么场景下需要约束

AI完全按我们的思路来生成

例如本期工作流中对于文戏和武戏

使用了不同细致程度的提示词

好的以上就是这期工作流的完整讲解

具体的部署步骤

需要注意的细节

还有邀请链接等

这些内容

我都已经详细地写进部署教程文档里了

我们实际操作部署的时候

一定要参照教程文档来操作

避免踩坑

后面我也会继续免费分享

更多实用的N8N工作流和skill

喜欢这类内容的话

可以点个关注

那我们下期再见
